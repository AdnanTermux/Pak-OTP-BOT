# ─────────────────────────────────────────────────────────────────────────
#  dex.txt  —  Sigma Fetcher V10  |  Login Panel Credentials
#
#  FORMAT
#  ──────
#  panel=<Name>
#  PANEL_BASE_URL = "<http://ip/ints>"   ← include /ints, NO trailing slash
#  PANEL_USERNAME = "<username>"
#  PANEL_PASSWORD = "<password>"
#
#  NOTES
#  ──────
#  • This file is read ONCE on first startup (when the panels table is empty).
#    After that, manage panels from the bot's Admin Panel → 🔌 Panels.
#  • URLs have NO trailing slash. The bot appends /login, /SMSCDRStats etc.
#    itself — a trailing slash would create double-slashes like /ints//login.
#  • Two accounts on the same server (same base_url, different credentials)
#    are fully isolated — each gets its own HTTP session and cookie jar.
# ─────────────────────────────────────────────────────────────────────────

